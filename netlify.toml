[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

# Official Next.js runtime — handles SSR, API routes (app/api), and image optimization.
[[plugins]]
  package = "@netlify/plugin-nextjs"
